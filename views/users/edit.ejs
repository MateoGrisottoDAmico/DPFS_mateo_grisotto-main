<!DOCTYPE html>
<html lang="es">
    <%- include("../partials/head.ejs") %>
<body>
    <%- include("../partials/header.ejs") %>
    <main style="display: flex; justify-content: center; margin: 8vw 0; align-items: center; flex-direction: column;">    
        <div class="inner-titulo">
            <h2>Editar perfil</h2>
        </div>
        <form method="post" action="/users/edit/<%= userLogged.id %>?_method=PUT" class="formulario" enctype="multipart/form-data">
            <div class="form-group">
                <label for="nombre">Nombre:</label>
                <input type="text" id="nombre" name="nombre" value="<%= userLogged.nombre%>" placeholder="Ingresa tu nombre" required>
            </div>
        
            <div class="form-group">
                <label for="apellido">Apellido:</label>
                <input type="text" id="apellido" name="apellido" value="<%= userLogged.apellido%>" placeholder="Ingresa tu apellido" required>
            </div>
        
            <div class="form-group">
                <label for="telefono">Teléfono:</label>
                <input type="number" id="telefono" name="telefono" value="<%= userLogged.telefono%>" placeholder="Ingresa tu teléfono" required>
            </div>
        
            <div class="form-group textarea-group">
                <label for="email">Email:</label>
                <input type="text" id="email" name="email" value="<%= userLogged.email%>" placeholder="Ingresa tu Email" required>
            </div>

            <div class="form-group textarea-group">
                <label for="password">Contraseña:</label>
                <input type="password" id="password" name="password" value="<%= userLogged.password%>" placeholder="Crea una contraseña" required>
            </div>

            <div class="form-group textarea-group">
                <label for="foto">Foto de perfil:</label>
                <input type="file" id="foto" name="foto" placeholder="Subir archivo" accept="image/*">
            </div>
        
            <div class="form-group">
                <button class="btn" type="submit">Enviar</button>
                <button class="btn" type="button" style="margin-top: 7px;" onclick="window.location.href='/'">Cancelar</button>
            </div>
        </form>
    </main>
    <script src="../../app.js"></script>
</body>
</html>